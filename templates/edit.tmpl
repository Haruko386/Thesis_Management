<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit - PaperHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen py-10 flex items-center justify-center px-4">

<div class="bg-white rounded-2xl shadow-xl border border-gray-100 w-full max-w-5xl flex flex-col md:flex-row overflow-hidden">

    <div class="w-full md:w-1/3 bg-gray-100 border-b md:border-b-0 md:border-r border-gray-200 p-8 flex flex-col justify-center items-center">
        <h3 class="text-lg font-semibold text-gray-700 mb-6">Current Cover</h3>

        <div class="relative w-full aspect-[1/1.414] bg-white shadow-lg rounded-sm border border-gray-300 flex items-center justify-center overflow-hidden">
            <img src="/paper/cover/{{.Thesis.ID}}" alt="Paper Cover" class="w-full h-full object-contain">
        </div>
    </div>

    <div class="w-full md:w-2/3 p-8">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-gray-800">Edit Paper</h2>
            <span class="text-xs font-mono text-gray-400 bg-gray-100 px-2 py-1 rounded">ID: {{.Thesis.ID}}</span>
        </div>

        <form action="/api/edit/{{.Thesis.ID}}" method="POST" enctype="multipart/form-data" class="space-y-5">

            <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                <div class="md:col-span-2">
                    <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Title</label>
                    <input type="text" id="title" name="title" placeholder="Paper Title"
                           class="w-full border border-gray-300 px-4 py-2.5 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition"
                           value="{{.Thesis.Title}}" required>
                </div>

                <div>
                    <label for="author" class="block text-sm font-medium text-gray-700 mb-1">Author</label>
                    <input type="text" id="author" name="author" placeholder="Author Name"
                           class="w-full border border-gray-300 px-4 py-2.5 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition"
                           value="{{.Thesis.Author}}" required>
                </div>

                <div>
                    <label for="classification" class="block text-sm font-medium text-gray-700 mb-1">Classification</label>
                    <input type="text" id="classification" name="classification" placeholder="e.g. AI; Science"
                           class="w-full border border-gray-300 px-4 py-2.5 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition"
                           value="{{.Thesis.Classification}}" required>
                </div>

                <div>
                    <label for="journal" class="block text-sm font-medium text-gray-700 mb-1">Journal</label>
                    <input type="text" id="journal" name="journal" placeholder="Journal Name"
                           class="w-full border border-gray-300 px-4 py-2.5 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition"
                           value="{{.Thesis.Journal}}" required>
                </div>

                <div>
                    <label for="publicdate" class="block text-sm font-medium text-gray-700 mb-1">Public Date</label>
                    <input type="date" id="publicdate" name="publicdate"
                           class="w-full border border-gray-300 px-4 py-2.5 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 outline-none transition"
                           value="{{.Thesis.PublicDate.Format "2006-01-02"}}" required>
                </div>
            </div>

            <div class="pt-6 flex gap-4">
                <button type="submit" class="flex-1 bg-blue-600 text-white py-3 rounded-xl font-bold shadow-md hover:bg-blue-700 hover:shadow-lg transition">Save Changes</button>
                <a href="/" class="px-6 py-3 rounded-xl font-semibold text-gray-500 hover:bg-gray-100 hover:text-gray-700 transition flex items-center justify-center">Cancel</a>
            </div>
        </form>
    </div>
</div>

</body>
</html>